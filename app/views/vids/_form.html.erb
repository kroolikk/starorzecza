<%= form_for(@vid, :html => {:multipart => true}) do |f| %>
  <% if @vid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vid.errors.count, "error") %> prohibited this vid from being saved:</h2>

      <ul>
      <% @vid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :title, "Tytuł" %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :fileurl, "Kod embedu" %>
    <%= f.text_area :fileurl %>
  </div>
  
  <div class="field">
    <%= f.label :description, "Opis" %>
    <%= f.text_area :description, :class => 'mceEditor' %>
  </div>
  
  <div class="actions">
    <%= f.submit "Zapisz" %>
  </div>
<% end %>
