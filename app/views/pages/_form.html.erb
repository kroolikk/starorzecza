<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>
      
      <ul>
	<% @page.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
	<% end %>
      </ul>
    </div>
  <% end %>



  <%= f.hidden_field :label, :value => @page.label %>
  
  <div class="field">
    <%= f.label :title, "Tytuł" %>
    <%= f.text_field :title %>
  </div>
  
  <div class="field">
    <%= f.label :content, "Treść" %><br />
    <%= f.tinymce :content %>
  </div>
  <div class="actions">
    <%= f.submit "Zatwierdź" %>
  </div>
<% end %>
