<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>
      
      <ul>
	<% @page.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
	<% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title, "Url" %>
    <%= "#{request.protocol}#{request.host_with_port}/" %><%= f.text_field :label %>
  </div>  
  
  <div class="field">
    <%= f.label :title, "Tytuł" %>
    <%= f.text_field :title %>
  </div>
  
  <div class="field">
    <%= f.label :content, "Treść" %><br />
    <%= f.tinymce :content %>
  </div>
  <div class="actions">
    <%= f.submit "Zatwierdź" %>
  </div>
<% end %>
