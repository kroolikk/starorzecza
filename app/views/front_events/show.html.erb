<div class="single-news">
  <h3>
    <%= @event.title %>
  </h3>
  <div>
    <%= raw @event.content %>
  </div>

  <div class="data">
    <span class="time">
      <i></i>
      <%= @event.created_at.strftime("%d-%m-%Y %H:%M:%S") %>
    </span>
  </div>
</div>
